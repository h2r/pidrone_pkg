Run offboard: put utils, offboard_slam, and slam_helper on the offboard computer and run offboard_slam and run image_pub in window 4 on the pi

Run onboard: put utils, slam, and slam helper on the pi and run slam in window 4
